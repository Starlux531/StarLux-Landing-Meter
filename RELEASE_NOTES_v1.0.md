# StarLux Landing Meter 1.0 正式版

1.0 是首个面向日常使用的正式版本，整合了 0.8 系列的测量算法与 0.9 系列的日志管理、浏览器可视化功能。

## 重点更新

- 完成 FPM、垂直投影 G、冲量等效 G 与速度变化的交叉验证流程
- 新增 100 ft 拉平轨迹、拉平曲率和轨迹震荡结论
- 新增 Bounce Landing 检测及对应评级处理
- 日志文件名包含机场、机型和推算跑道
- 新增游戏内落地记录管理、浏览器可视化和本地 TXT 下载
- 新增风向、接地姿态、50 ft 参考线和可用时的跑道触地点示意
- 新增独立报告阅读器的数据对比功能
- 插件内打开报告与独立阅读器统一使用同一份 HTML 界面
- 将“重着陆”调整为“不良落地”，评分阈值和算法保持不变
- 合并独立阅读器说明到总使用说明
- 正式发布包不再包含个人测试日志

## 安装文件

将压缩包内以下内容复制到 FlyWithLua 的 `Scripts` 文件夹：

```text
StarLux_LMM_v1.0.lua
LMM_Report_Reader.html
LMM_Settings.cfg
LMM_Log/
```

请先移走旧版 Lua，避免多个版本同时运行。

## 阅读器同步机制

`LMM_Report_Reader.html` 是 1.0 唯一的阅读器界面来源。游戏内记录菜单打开日志时，Lua 会在 `LMM_Log` 中生成本地桥接数据和阅读器副本，自动载入所选日志。独立打开时，则可以手动选择多份日志进行复盘和对比。

所有数据仅在本机处理，不上传网络。

## 兼容与限制

- 适用于 X-Plane 12 与 FlyWithLua NG+
- 可读取项目早期日志以及 0.7～1.0 日志
- 早期日志缺少拉平采样时，仍可查看评分、触地数据和原文，但无法绘制 100 ft 轨迹
- 跑道号仍由触地磁航向推算，不区分平行跑道 L/R/C
- “UNSTABLE 不良落地”是插件评分表述，不替代航司、维修或适航判定

## 校验

正式版发布前已执行：

- Lua 静态完整性检查
- 阅读器 JavaScript 语法检查
- 多份真实日志解析
- 主数据／对比数据选择、交换、取消对比测试
- Lua 本地桥接自动载入测试
- 发布包文件清单与版本号检查
