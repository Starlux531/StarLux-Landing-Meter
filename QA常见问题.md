# QA 常见问题

> 本文用于回答 StarLux Landing Meter（下文简称 LMM）上线后常见的技术疑问，并会随着测试数据、用户反馈和版本更新持续补充。<br>
> 当前对应版本：**1.0**<br>
> 最后更新：**2026-07-27**

## 阅读前先说明

LMM 是一款面向 X-Plane 12 与 FlyWithLua 的模拟飞行落地分析工具，不是经航空器制造商、航空公司或适航机构认证的飞行数据记录设备，也不能代替真实飞机的 FDR/QAR、航司 FOQA/FDA 系统或工程检查。

因此，我们不会用一个缺少真实“标准答案”的百分比，宣传所谓“99% 准确率”。LMM 真正努力保证的是：

- 同一套输入能够得到可重复的结果；
- 关键结果有原始样本、计算过程和置信度支撑；
- FPM、G 值、拉平轨迹与弹跳等指标不被强行拼成一个看似漂亮的数字；
- 算法、阈值与版本变化可以公开检查；
- 当数据质量不足时，主动降级处理，而不是把不可靠结果包装成高精度结论。

---

## 1. 这个插件的准确率该如何保证？如何让大家信服这个插件的数据？

### 简短回答

LMM 不靠“相信作者”来证明准确，而是尽量让一次落地的结果能够被**追溯、复算、交叉检查和重复验证**。

### “准确”需要分成三个层次

1. **采样准确性**<br>
   插件有没有及时、连续地读取 X-Plane 提供的数据，采样间隔是否正常，是否出现掉帧或缺样。

2. **算法一致性**<br>
   相同的样本进入相同版本的算法后，是否会得到相同结果；日志中的样本和公式能否重新算出报告结论。

3. **模拟真实性**<br>
   X-Plane 与当前机模提供的数据，是否足够接近真实飞机。这个层次不仅由 LMM 决定，还取决于 X-Plane 飞行模型、机模起落架调校、质量与重心、插件冲突、帧率以及机模是否使用自定义系统。

LMM 能直接控制前两项，但不能替所有第三方机模保证第三项。因此，“插件读数忠实于模拟器”与“模拟器完全复现真实飞机”是两件不同的事。

### LMM 目前采用的可信度措施

- **触地不是靠猜测 RA=0**：以 X-Plane 的离地/接地状态发生切换作为第一次触地事件。
- **FPM 不读取单独一帧**：主要使用触地前 250 ms 内的离地物理垂直速度样本，并采用第 25 百分位，减少接地信号延迟和末端样本接近零造成的失真。
- **G 值不直接由 FPM 捏造**：记录第一次起落架压缩过程中的过载曲线，并结合压缩前后垂直速度变化、冲量一致性、持续时间与样本完整度判断采用哪一种结果。
- **抑制单帧尖峰**：曲线百分位、持续时间和冲量校验共同参与判断，避免把一次异常尖峰直接当成整次落地。
- **FPM 与 G 独立分档**：两项分别评级，最终采用较严重的一项。低 G 不会把明显偏高的 FPM“抵消”，反之亦然。
- **完整数学记录可选**：开启后会保存关键时间窗、排序样本、百分位索引、积分过程、中间量、最终选值方法和置信度，用户可以逆向复算。
- **源码公开**：当前实现可直接在 [`StarLux_LMM_v1.0.lua`](StarLux_LMM_v1.0.lua) 中检查。

这种思路与真实世界飞行数据分析强调的“数据质量、定义维护、验证和趋势复查”更接近，而不是只公布一个无法核验的结论。FAA 的 FOQA 指南同样把数据质量与完整性检查、事件定义验证、变更记录和趋势复查列为重要环节。

### 它仍然有哪些误差来源？

- 落地瞬间帧率过低或采样间隔异常；
- 第三方机模的起落架、过载或接地状态建模不准确；
- 机模自定义仪表与 X-Plane 通用 DataRef 使用了不同滤波或计算方式；
- 回放模式、暂停、时间倍率或其他 Lua/原生插件改变数据；
- X-Plane 本身与真实飞机之间的模型差异。

所以，最有说服力的证据不是“一次读数刚好符合感觉”，而是大量可复算记录在不同机型、不同帧率和不同落地条件下仍保持稳定，并且异常案例能够找到明确原因。

---

## 2. 为什么有些飞机即使落比较高的 FPM 值，G 值仍然偏低，例如某些 A339 机模？

### 简短回答

因为 **FPM 描述接地前的垂直速度，G 值描述这段垂直速度在接地后以多快、多强的方式被改变**。两者有关联，但不存在固定的一一对应关系。

可以用一个经过简化的概念式理解：

```text
附加过载大致取决于：垂直速度变化量 ÷ 减速时间
```

也就是：

```text
G ≈ 接地前基线G + ΔV ÷ (g × Δt)
```

这只是帮助理解的近似关系，不是 LMM 完整算法的全部公式。它说明了一个关键事实：同样的接地垂直速度，如果起落架用更长时间和更长行程吸收能量，垂直减速度峰值可能更低；如果减速过程很短、很尖锐，即使 FPM 不高，也可能出现较高 G 值。

### 还会受哪些因素影响？

- 起落架行程、阻尼、轮胎变形和机模的接地动力学；
- 飞机质量、重心和惯性；
- 主轮接地时的俯仰、横滚与左右轮先后顺序；
- 机翼在接地阶段仍提供的升力；
- 机模如何生成法向过载、轮载和接地状态；
- 插件测量的是模拟器提供的机体过载响应，不是轮胎局部载荷，也不等于乘员身体某个位置感受到的瞬时冲击。

NASA 的起落架动力学研究会分别考察轮胎力—变形、起落架摩擦、冲击速度和机体不同位置的加速度响应。这也说明“接地速度”和“机体感受到的载荷”本来就是相关但不同的量。

### 那么某些 A339 的“高 FPM、低 G”一定正确吗？

不一定，也不能只凭机型名称下结论。它可能反映了该机模较柔和或较长的起落架压缩过程，也可能来自机模的过载输出、接地判据或调校差异。判断时应查看完整数学日志中的：

- 触地前物理 FPM；
- 接地前基线 G；
- 第一次压缩曲线 G；
- 压缩持续时间；
- 压缩前后垂直速度变化；
- 冲量一致性误差、样本间隔和置信度。

如果同一机模长期、系统性地出现不合理组合，应该收集多次记录做机型级验证，而不是强行让公式把 FPM 和 G 修成相同等级。

---

## 3. 为什么我目测的机载 VS 值与本插件测出来的值不符？应该相信哪一个？

### 简短回答

两者测量的通常不是同一个“时刻”和同一种“数据表现”。机载 VS 更适合回答“飞行员当时在仪表上看到了什么”，LMM 更适合回答“触地前短时间窗内，模拟器中的飞机实际如何运动”。它们可以同时正确，但用途不同。

### LMM 到底如何判断触地？

1. 插件持续记录近地阶段数据；
2. 当 X-Plane 的接地状态从“离地”切换为“接地”，且地速大于 35 kt 时，记录第一次触地时刻；
3. 最终 FPM 主要取自该时刻之前 250 ms 的 `local_vy` 物理垂直速度窗口，而不是等待无线电高度显示 0，也不是截图中某一帧的 VS 指针值。

因此，“触地时 RA 可能不等于 0”并不会让 LMM 错过触地。LMM 的 100 ft/50 ft 轨迹参考使用的是飞行模型 AGL 高度；它也不等同于每一架第三方飞机 PFD 上经过天线位置和零位校准后的无线电高度。

X-Plane 官方说明，X-Plane 12 的无线电高度读数会受到天线安装位置、俯仰姿态和零位偏置影响。以 737 为例，主轮接地时可以校准为 0，而飞机减震支柱压缩、机头放下后甚至可能显示负值。官方也明确建议 PFD 使用专门的驾驶舱无线电高度 DataRef，而不是飞行模型高度。

### 为什么机载 VS 会不同？

- **显示滞后或平滑**：传统 VSI 本身具有滞后；数字化或自定义 PFD 也可能进行平滑、限幅和舍入。FAA 的仪表飞行资料明确指出，传统 VSI 存在响应滞后，而 IVSI 才通过加速度补偿来减少这种滞后。
- **读数时间不同**：人眼看到或视频暂停到的那一帧，未必正好是主轮第一次接地前的 250 ms。
- **数据源不同**：第三方机模的 PFD 可能使用自定义 DataRef 或内部滤波；LMM 使用 X-Plane 的物理垂直速度 `local_vy`，同时把通用 VVI 作为诊断参考。
- **显示分辨率不同**：仪表刻度、刷新率和整数化会丢失短时间变化，插件则保存连续样本并进行稳健统计。
- **回放并非测量基准**：视频帧率、回放插值和暂停位置都会造成目测偏差。

### 最终应该相信谁？

| 你想回答的问题 | 更合适的依据 |
| --- | --- |
| 飞行员当时在驾驶舱里看到了什么 | 机载 PFD/VSI |
| 飞机在第一次主轮接地前的物理下沉速度 | LMM 的触地前物理 FPM 与完整样本 |
| 机模的仪表滤波是否与飞行模型一致 | 同时对比 PFD、`local_vy`、通用 VVI 与详细日志 |
| 真实飞机是否需要工程或维修检查 | 真实制造商程序、机载记录与维护数据；不能由 LMM 判断 |

如果两者差异很大，请优先保留完整数学日志，而不是只提供一张仪表截图。日志可以检查时间窗、样本间隔和数据源；截图只能说明某一画面显示了什么。

---

## 4. 如果大家都习惯了 FPM 与 G 值完全对应，这个插件可以改变这种刻板印象吗？

### 简短回答

可以帮助大家重新理解，但不应该靠口号强迫用户接受。最好的办法是让每一份报告自己提供证据。

“FPM 越大，G 一定按固定比例越大”不是通用物理定律。FPM 是接地前的速度状态，G 是接地减速与机体响应；中间还隔着起落架吸能时间、飞机姿态、升力、质量、惯性和机模实现。

市面上有些工具会主动让两者匹配。例如开源 XGS 的说明明确写道，它的 G 值由垂直速度的导数计算，因此两个值会匹配。这是一种明确且可理解的设计选择，并不等于程序出错；但这种 G 值与 FPM 天生来自同一条计算链，不能再被当作一项完全独立的佐证。

LMM 选择另一条路线：

- FPM 从触地前物理垂直速度窗口获得；
- G 从第一次起落架压缩的过载曲线与垂直冲量获得；
- 两者分别评级，采用较严重结果；
- 报告解释两者为什么一致，或者为什么不一致。

这会产生一些不那么“整齐”的组合，例如低 FPM/高 G，或高 FPM/低 G。但只要原始样本、时间窗和计算过程合理，这种“不整齐”反而更接近真实动力学问题，而不是算法失败。

LMM 想改变的不是玩家对“好落地”的审美，而是把讨论从“两个数字看起来配不配”推进到：

- 接地前到底有多大的垂直速度；
- 这段速度用了多长时间被起落架吸收；
- 过载是持续的还是单帧尖峰；
- 拉平轨迹是否稳定；
- 是否发生弹跳、二次触地或数据质量下降。

---

## 5. 与市面上的落地率插件相比，有什么根本不同？解决了什么痛点？

### 简短回答

LMM 的定位不是“再弹出一个 FPM 数字”，而是用尽量低的使用门槛，建立从**近地采样、触地识别、结果评分、证据保存到可视化复盘**的一条完整链路。

需要说明的是，市面上并非所有工具都只读取一帧，也并非所有工具都很吃性能。已有工具中既有轻量落地率记录器，也有包含曲线、地图和完整飞行数据监控的高级方案。LMM 不宣称自己是唯一或绝对最好，而是选择了一个不同的平衡点。

### 主要区别

| 方向 | LMM 的处理方式 |
| --- | --- |
| 触地结果 | 不只保存一个瞬时数字，而是分析触地前后时间窗 |
| FPM 与 G | 两套相互校验但保持独立的数据链，不强制匹配 |
| 拉平过程 | 从约 100 ft 开始采样，并在可视化中标注 50 ft 参考线 |
| 弹跳 | 识别持续离地与第二次触地，保存两次触地信息并按规则修正评级 |
| 可解释性 | 可选完整数学记录，保存样本、排序、百分位、积分、中间量与置信度 |
| 历史管理 | 在插件菜单中查看近期记录、打开报告或删除存档 |
| 可视化 | 本地浏览器报告展示评分、轨迹、姿态、风向、跑道上下文和记录对比 |
| 独立使用 | 可以不启动模拟器，直接用独立阅读器载入和比较已有 TXT 报告 |
| 隐私 | 报告默认保存在本机，不要求账号或上传云端 |
| 性能策略 | 近地阶段使用固定容量缓冲区采样，把机场识别、复杂分析和文档生成分阶段延后 |

### 它主要解决的痛点

1. **数字有了，但不知道怎么来的**<br>
   通过可选数学记录和置信度，把“黑箱结论”变成可以检查的证据链。

2. **FPM 与 G 不一致时，只能怀疑插件坏了**<br>
   保留两者的独立意义，并用压缩时间、冲量和过载曲线解释差异。

3. **只知道这次落砸了，不知道拉平哪里出了问题**<br>
   保存 100 ft 后的下降率、速度与姿态轨迹，辅助观察拉平时机、震荡和能量变化。

4. **弹跳被一个最终数字掩盖**<br>
   单独记录离地过程与第二次触地，并在评级中体现弹跳影响。

5. **记录散落在文件夹里，复盘和分享麻烦**<br>
   提供本地可视化、历史管理、原文下载和双报告对比。

6. **为了复盘还要长期运行额外客户端或上传数据**<br>
   核心采样在 FlyWithLua 内完成，报告与阅读器都可以离线使用。

LMM 并不打算替代完整的稳定进近监控系统、全航程 FDM 或航司训练平台。它专注的是普通玩家最常用的“近地—触地—复盘”流程，在专业性、可读性和运行负担之间取得平衡。

---

## 发现疑似异常时，请尽量提供这些信息

为了让问题可以复现和定位，请不要只提供最终的 FPM/G 截图。建议同时附上：

- X-Plane、FlyWithLua 与 LMM 的版本；
- 机型、具体机模名称和机模版本；
- 是正常飞行还是回放模式，是否使用暂停或时间倍率；
- 触地前后的大致帧率；
- 对应的 TXT 报告，最好开启“完整数学记录”；
- 机场、跑道、天气和道面情况；
- 是否同时运行其他落地率、相机、回放或飞行模型相关插件；
- 你认为异常的具体字段，以及预期它应该是多少；
- 视频或截图可作为辅助，但不能替代原始报告。

对于同一机模的系统性偏差，三至五份不同落地条件下的完整报告，通常比单次案例更有判断价值。

---

## 参考资料

以下资料用于解释方法论和模拟器数据边界，不代表相关机构为本插件背书：

- [X-Plane Developer：Radio altimeter sensor location and offset](https://developer.x-plane.com/article/radio-altimeter-sensor-location-and-offset/)
- [X-Plane Developer：Moving the Plane（`local_vy` 与派生垂直速度）](https://developer.x-plane.com/article/movingtheplane/)
- [FAA：Instrument Flying Handbook（VSI 滞后与 IVSI）](https://www.faa.gov/sites/faa.gov/files/pilots/FAA-H-8083-15B.pdf)
- [FAA：AC 120-82 — Flight Operational Quality Assurance](https://www.faa.gov/regulations_policies/advisory_circulars/index.cfm/go/document.information/documentID/23227)
- [NASA NTRS：Experimental and analytical determination of characteristics affecting light aircraft landing-gear dynamics](https://ntrs.nasa.gov/archive/nasa/casi.ntrs.nasa.gov/19780003109.pdf)
- [XGS 开源项目说明](https://github.com/hotbso/xgs)
- [LRM 官方功能说明](https://www.lrmclient.org/)
- [MSFS Landing Inspector 开源项目](https://github.com/mracko/MSFS-Landing-Inspector)
- [StableApproach 项目说明](https://github.com/Clamb94/StableApproach)

---

## 更新记录

| 日期 | 内容 |
| --- | --- |
| 2026-07-27 | 创建初版，回答准确性、FPM/G 关系、机载 VS 差异、常见认知误区与市场定位问题。 |
