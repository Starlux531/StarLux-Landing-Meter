# StarLux Landing Meter 路线图

当前正式版：**1.1.9，2026-09-23**。该开发周期以 beta10 定稿结束，连续录制、全程风与操纵复盘、ILS 几何参考、长弹跳监测、滑跑评分、覆盖层拖动 / 缩放 / 折叠与反推显示已纳入正式版。AP / AT 监测已撤回；横滚虚线使用两侧外段。

正式包位于 `dist/1.1.9/`；安装器独立通道 v1.0.0 保持已发布状态，离线包内置维护版 1.0.1。开发历史和离线验证范围见 [开发验证](docs/StarLux_LMM_v1.1.9_开发验证.md)。

下一阶段保留：大盘分析独立验证模块的完善与集成、FF777 等专有操纵来源适配、游戏内实时 G 曲线、更多真实机模 / 高 DPI / 多显示器场景验证。下文各 beta / 日期段为历史规格和检查记录，不代表仍等待开发全部已交付功能。

## 2026-09-21 交互修订（当前执行规格）

- 安装器本地维护版 **1.0.1**：旧版 LMM UI 文件、空目录和可确认属于 StarLux 的版本 README 纳入事务备份及清理；保留设置、飞行记录、机场缓存和未知用户文件。1.1.8 及已发布安装器资产保持不变。
- 接地弹窗标准／兼容界面均可拖动，松开后保存归一化位置；选九宫格或复位可回到锚点。
- 取消独立“完整记录 · 进近、滑跑与风”面板。完整记录接入原图表、仪表和播放时间轴，支持 100 ft 聚焦、全程、缩放、横向浏览及播放跟随。主图风参数仅保留风速；风向／迎风／侧风移至落地俯视图旁的可选竖向风图，使用同一时间窗和游标。
- “触地俯视图”叠加真实滑跑路线；“落地俯视图”显示全进近路线、风吹向和同步位置，支持 1–64 倍显式缩放，放大后保留浅色风箭头。延长中线是几何参考，并非实际 LOC 接收信号。
- **删除出口网络判断。接地后在地面且 GS < 30 kt 连续两秒即结束，原因 `low_speed`；无机场网络也能完成。GS ≥ 30、离地、暂停或数据间隙重置计时。** 该状态表示完成低速录制，不代表确认脱离跑道；旧“出口证据＋超过三秒”要求取消。
- 保留已录样本；新增完整段物理 FPM／VVI／AoA 和 `LMM_RUNWAY` 真坐标参考。旧日志缺失字段留空，能可靠对齐的 100 ft 节选提供补充。完整段高度明确标为地形 AGL。新增全程轨迹 FPM 及来源、六个舵面、油门杆与卡位通道；已验证海拔 6000 ft 机场的 AGL 启动及低空载入。
- XYZ 操纵图增加相对机头风吹向及风向／风速读数；Ctrl＋滚轮、缩放按钮或倍率选择是缩放入口，悬停不改变视区和纵轴范围。覆盖层取消固定 20 Hz 限制、按帧刷新并避免重复设置窗口几何；45 项 Lua 离线回归及浏览器交互验证通过，实机流畅度仍待验收。

## 已发布：安装器 v1.0

- [x] 安装器独立更新通道、版本发现、下载校验、替换 EXE、重启确认与失败回滚。
- [x] 插件文件诊断：缺失、内容不符、多个主脚本、旧 UI 残留及 XP 版本兼容性；未知状态不误报通过。
- [x] 修复时匹配兼容包，提供保留配置或纯净重装；保留飞行记录和机场缓存。
- [x] 插件卸载、事务备份及恢复，不卸载共享 FlyWithLua 或其他脚本。
- [x] 顶部中英切换、蓝色渐变界面、独立状态卡；更新红色闪烁，确认最新时静态绿色。
- [x] 临时目录中的维护回归和真实 EXE 替换／重启／失败回滚检查。
- [x] 发布 `installer-v1.0.0` 和配套更新资产，校验公开目录识别规则与远端文件哈希；1.1.8 EN／CN 离线包内置安装器 v1.0，插件 v1.1.8 保持 Latest。
- [ ] 继续收集用户实际使用反馈；离线回归和发布校验不替代全部真实环境验收。

安装器首个正式版本配合已完成的插件 1.1.8 使用。插件 1.1.9 不随安装器升级自动发布。构建与更新协议见 [安装器开发说明](installer/README.md)。

## 已完成的阶段

### 1.1.9-beta7：风箭头、ILS 展示与长弹跳

- [x] 风向箭头固定长度，保留风速颜色分级；实时覆盖层移除全部 ILS 显示。
- [x] 分析器 LOC／GS 仪表固定尺寸、始终占位；无有效参考时显示 INOP。
- [x] 弹跳监测解除 6 秒和短轨迹窗口绑定，保留连续性／抖动过滤、低速结束与复飞区分。
- [x] 最终摘要刷新，旧完整记录补充弹跳证据但保留原评分；实飞样本与长弹跳离线回归。
- [ ] beta7 实机试飞验收：长弹跳、连续弹跳、复飞、低速结束与即时弹窗。

### 1.1.9-beta6：正方形操纵区域

- [x] 修正 beta5 操纵区长宽不等，所有组件尺寸和调试模式下保持横纵比例 1:1。
- [x] 保留紧凑信息排布与缩放功能，LOC／GS 紧邻正方形操作区。

### 1.1.9-beta5：输入布局与边缘缩放

- [x] 扩大十字操纵区，独立保留风速、YAW、LOC／GS 空间，保持风箭头角度。
- [x] 标准版摇杆／油门／N1、落地弹窗四边／四角拖拽缩放，最小／最大尺寸和桌面边界限制。
- [x] 保留传统尺寸控件、位置拖动、调试来源切换；松开鼠标保存。
- [ ] X-Plane 实机确认多显示器／高 DPI 下鼠标命中、字体与拖拽手感。

### 1.1.9-beta4：竖向区间细节与分析器性能

- [x] 竖向图默认保留，LOC／跑道中线偏差按区间放大，保留风向切换与明确缩放控制。
- [x] 鼠标移动复用静态图表、合并指针事件、缓存航迹并使用空间索引，保持时间轴和仪表联动。
- [x] 浏览器工作量、缩放游标对齐、窗口播放跟随、旧文件导入与发行包检查通过；记录离线前后基准。

### 1.1.9-beta3：ILS 参考复盘

- [x] XP 导航资料的 LOC／GS 几何参考、分块索引与缓存，录制保存参考快照；不依赖机模收音机接口。
- [x] 落地俯视图绘制 LOC 参考线，竖向蓝线改为飞机 LOC 偏差，风仅在飞机位置绘制箭头。
- [x] 网页及原生操纵组件的四点 LOC／GS；无设施分别隐藏，风箭头按 10／20 kt 分色并调淡。
- [x] 俯视图悬停同步时间轴，保持明确缩放控制；旧记录兼容、离线数值／浏览器／发行验证。
- [ ] 实机跑道匹配、机载位置与复盘对照、帧耗验收；当前只作几何复盘，不直接改变评分。

### v1.1.8：现有功能与稳定基线

- 已完成落地采集与复盘、操纵输入和发动机输出记录、机场索引及历史报告兼容能力。
- 已完成标准版原生中英文 UI，以及旧版 UI 兼容通道和 CN／International 默认语言版本。
- 已完成安装器、独立网页分析器、同步仪表与曲线播放、作者配色预设及使用文档。

具体变更见 [CHANGELOG](CHANGELOG.md)。1.1.9 将在此基础上开发，继续保留现有落地分析能力。

### v1.1.9：工作区与开发基线准备

- [x] 归档历史版本、发布包和临时开发资料，保存完整 1.1.8 源码快照与文件校验清单。
- [x] 建立唯一的顶层主脚本 `StarLux_LMM_v1.1.9.lua` 和配套 `LMM_UI_119/`，同步构建、测试和安装器引用。
- [x] 保留原有算法、网页、字体、预设及用户配置和日志，记录新版本开发计划。
- [x] 完成基线离线检查：17 项 LuaJIT/UI 回归、网页检查与构建脚本语法检查通过。

当前已实现覆盖层和本地 1.1.9 开发包，尚未正式发布或进行实机验证。安装器 1.0.1 维护修订随本地开发包验证。工作区与检查说明见 [WORKSPACE](docs/WORKSPACE.md)。

## v1.1.9：连续录制与滑跑路线评分（新增重点）

### 1. 从 2500 英尺进近录制至跑道脱离

- [ ] 在下降进近进入 2500 英尺时开始记录，贯穿拉平、接地、弹跳、减速滑跑和脱离；保存实际经纬度路线及同步操纵、发动机、姿态等数据。
- [ ] 自动存档恢复／低高度载入兜底：无需再次穿越 2500 ft，从载入后的首个有效进近样本开始，标记实际起始高度及缺失区间；已有块只在会话与连续性可验证时关联，读档回退建立新尝试，不拼接旧未来。缺段影响对应项目的可评估性，不直接扣分，也不伪造完整记录。
- [ ] 增加风场时间序列：整个录制期间每隔可配置的 x 秒保存飞机所在位置的风速、风向及侧风／顺逆风分量，与轨迹、操纵、姿态和 G 值同步回放；保留区间风变化及峰值，避免只描述落地瞬间。具体间隔待验证，风环境与飞机响应联合展示，风导致的影响量不作未经标定的因果推断。
- [x] 接地后在地面，地速严格低于 30 kt 连续两秒结束，记录 `low_speed` 原因；取消出口匹配和三秒确认。
- [x] 无机场网络仍可低速完成；回到 30 kt、离地、暂停或数据间隙重置计时，不把低速结束称为正常脱离。
- [ ] 即刻弹窗显示截至弹出时刻的数据和阶段评分，后台继续录制；最终记录保存完整滑跑及最终评分，清楚区分接地分析完成、录制完成和文件保存成功。
- [ ] 报告前部保留落地摘要与原有 100 ft 短窗节选，并标注“完整记录见末尾”；包含轨迹、操纵／动力、风和事件的完整数据层置于文件最后，数学复算附录位于其前。新解析器区分节选与全量，避免重复计点。
- [ ] 明确高度基准、复飞／连续起落、暂停／回放、插件重载、异常中断和缺失数据行为，保持旧报告可读。

### 2. 将滑跑路线纳入评分

> 2026-09-22 / beta8：已按用户指定的固定 3/5/7/9 m、5/3 s 与 GS ≥60 kt 规则实现并启用评分；以下早期探索条目中的宽度自适应阈值和复杂脱离识别不再作为本版实施方案。准确边界及叠加规则见 README_1.1.9.md 的 beta8 章节，实机验收仍待完成。

- [x] 固定阈值评分、单项一次触发、非红封顶黄色、红色恢复滞回。
- [x] 跑道延迟匹配样本补判、即时等级更新、最终中英文报告与分析器说明。
- [ ] 实飞验证正常回中、横风、摆动、长偏差、弹跳和低速脱离场景。


- [ ] 在有效的着陆滑跑段计算横向中线偏差、超限持续时间／距离，以及有明显幅度和持续性的左右摆动。
- [ ] 达到规则条件时降级，并在报告中标注原因、发生区间、指标值、阈值和规则版本。
- [ ] 识别正常脱离转弯、跑道头掉头与回滑，避免将这些动作当成滑跑失稳；正常转弯确认前保留待判数据，不能仅因转弯或低速免除已发生的偏差。
- [ ] 根据跑道宽度、速度阶段、位置质量及实际样本标定规则；不直接沿用现有触地点 7／15 米阈值作为全程滑跑标准，不重复叠加同一事件的处罚。

### 联合开发顺序与验收

1. 重跑基线并定义数据约定；先完成逐轴输入源、最小摇杆诊断覆盖层及两个贴边快捷入口，优先定位 FF777 摇杆缺失。
2. 完成长时录制、风时间序列与阶段快照，实现前部易读节选／末尾完整数据和分析器兼容。
3. 验证独立低速结束、真实跑道投影和统一时间轴报告闭环。
4. 记录滑跑指标和候选判定，样本标定后启用降级；补齐覆盖层仪表与布局功能。
5. 完成性能、标准／兼容 UI、机模实测与发行包回归；实时 G 曲线仍按验证结果决定纳入范围。

详细实现建议、涉及模块、性能预算和边界场景见 [1.1.9 开发计划](docs/StarLux_LMM_v1.1.9_开发计划.md)。阈值标定和实机验收仍待完成，邀请背景不代表已获得或采用航空公司正式评分标准。

## v1.1.9：游戏内实时覆盖层（保留计划）

### 产品目标

让玩家按需启用游戏内实时视图。参考网页版仪表的布局与配色，将信息拆成精致、独立的小组件，减少对驾驶舱仪表和外部视野的遮挡。组件应能分别启用、摆放和调整外观，并为后续新增覆盖层保留扩展能力。

### 功能范围与优先级

| 优先级 | 内容 | 当前状态 |
| --- | --- | --- |
| 首批核心 | 摇杆二维俯仰／横滚输入指示；方向舵输入条作为可选扩展 | 待开发 |
| 第一批诊断 | 调试模式逐轴显示当前输入来源／路径／有效性，并可切换来源或恢复自动；与记录器共用选择，保留切换事件，重点验证 FF777 | 待开发，FF777 问题未修复 |
| 第一批入口 | 两个低存在感的半透明贴边按钮，分别快速打开设置和记录栏；常规模式可用，支持隐藏、左右贴边及透明度设置 | 待开发 |
| 首批核心 | 油门输入与逐发动机 N1 输出，清晰区分输入、实际输出及发动机编号 | 待开发 |
| 首批核心 | 组件独立显示／隐藏、位置、大小和颜色调整 | 待开发 |
| 配套体验 | 透明度、布局保存与恢复默认；编辑时显示拖动／缩放控件，常规显示保持简洁 | 待开发 |
| 条件纳入 | 实时 G 值滚动曲线，提供清楚的时间窗、单位与纵轴 | 待验证数据与性能，确认后决定是否纳入 1.1.9 |
| 后续扩展 | 更多指标覆盖层，以及油门、N1、姿态等实时曲线 | 候选方向，暂不绑定版本 |

### 开发顺序与完成条件

1. **最小组件与诊断验证**：先实现摇杆观测组件及调试模式的来源查看／切换，复用设置和记录栏入口增加两个贴边按钮；验证命中区域与输入行为。第一批诊断可用后再补齐完整布局编辑。
2. **首批仪表**：完成摇杆、油门与 N1 组件，统一比例、字体、留白和颜色层级；检查常见机模的数据来源与缺失状态，避免把不可用值显示为 0。
3. **自定义与布局**：接入组件开关、大小、颜色、透明度及布局保存／恢复；验证重载后的设置继承、分辨率变化与窗口边界，评估锁定布局和多屏处理。
4. **实时曲线验证**：先用固定容量缓存实现 G 曲线原型，检查采样频率、时间窗、暂停／回放和数据断点；根据帧耗时与长时间运行结果确定首版范围。
5. **回归与发行准备**：验证标准版及兼容版的实际支持范围、双语显示、布局持久化和旧日志兼容；通过离线回归与实机检查后，重新构建安装器、校验发行包并更新说明。

以上为覆盖层内部开发顺序，整体排期按上方联合开发顺序执行。完整需求与实现约束见 [1.1.9 开发计划](docs/StarLux_LMM_v1.1.9_开发计划.md)。

### 数据与性能要求

- 复用已有操纵与发动机数据适配，明确数据来源、单位和缺失状态；不承诺尚未验证的全部机模支持。
- 全程实时观测需要独立于录制阶段缓存的采样生命周期；覆盖层开关不改变检测、采集、评分及保存行为。新增滑跑评分与报告扩展由上方独立需求控制。
- 调试模式为覆盖层增加来源信息和切换控件，普通模式仍可显示仪表；切换只影响后续采样并记录事件，不回写机模操纵数据，不修改已录历史。
- 实时 G 曲线的瞬时载荷与报告中的稳健接地 G 应有清楚区分。
- 采用限频采样和固定容量缓存，隐藏组件避免无效绘制，验证长航程内存占用及帧耗时。
- 优先验证现有 SDK 440 UI 的复用与旧版 UI 降级方案；VR、多屏和特殊机模能力以验证结果为准。

## 1.1.9 之后：按验证结果扩展

- 在独立组件和实时曲线基础稳定后，逐步扩充指标与显示样式，保持组件可单独启用。
- 根据实飞反馈完善机模适配、布局交互、遮挡控制和性能表现。
- 后续版本号与具体功能范围在 1.1.9 验证后确定；可移植重写、MSFS 和完整航程产品继续作为长期方向。

## 长期方向（未排期）

### 1. 脱离 FlyWithLua 的可移植重写

- 逐步淘汰 FlyWithLua 运行层，使用更适合跨平台和移植的语言重新编写程序。
- 将测量与评分核心、模拟器适配层、界面和文件层分离。
- 新界面不再受到 ImGui 字体与布局限制，可以使用更灵活的视觉和交互设计。
- 目标交付形态是尽可能少的独立插件文件，降低安装、升级和排错成本。

### 2. 移植至 Microsoft Flight Simulator

- 面向 MSFS 2020/2024 建立独立数据适配与校准体系，不直接复制未经验证的 X-Plane 数字。
- 保留项目核心竞争力：精准测量、便于复盘、数据来源透明、异常能够解释。
- X-Plane 与 MSFS 分别验证和标定，避免虚假的跨模拟器一致性。

### 3. Starlux Black Box

- 未来记录整段航程的飞机状态、操纵、性能、环境和关键事件数据，而不再局限于落地阶段。
- 研究接入现实航空公司模拟机训练与 QAR 风格的数据结构、事件规则和复盘流程。
- 面向专业航司、飞行学校和模拟舱训练提供可选的付费专业能力，包括组织标准、机型标定、训练事件和完整航程分析。
- 免费版本仍可完整使用落地记录功能；落地记录器将保持独立、可用且不设置功能残缺式付费墙。
- 新开发的完整航程产品工作名称为 **Starlux Black Box**，目标是在模拟飞行平台打造高透明度、可解释、便于训练复盘的黑匣子数据记录器。

## 长期不变的原则

- 不用一个无法解释的数字取代原始证据。
- 不为了跨平台数字一致而掩盖模拟器和机模差异。
- 免费落地记录功能保持完整。
- 专业付费功能与社区落地记录器清晰拆分。
- 真实航空公司/QAR 标准只能在完成授权、标定和验证后声明适用范围。
