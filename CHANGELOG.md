# Changelog

更新范围：**1.1.8 正式版 → 1.1.9 正式版**。整理日期：**2026-09-23**。

下文先汇总当前实际行为，再列出开发版本演进。插件 1.1.9 已确认正式发布；安装器采用独立版本号。1.1.8 及更早版本的原始更新记录保留在后文。

## 1.1.9 · 2026-09-23 · 正式版 / Stable

- 以 beta10 最终功能为正式基线：2500 ft AGL 连续录制、全程操纵与风、同步完整时间轴及俯视图、ILS 几何参考、延长弹跳监测、GS ≥60 kt 滑跑中心线考核。
- 保留可拖动 / 缩放 / 折叠覆盖层、实时风、外侧 Roll 虚线、鲜绿色操纵标记和反推 R / 红色 N1；不包含已撤回的 AP / AT 监测。
- 主脚本、报告、两种分析器、四种安装清单及文件名统一为 1.1.9；正式版排序高于同号所有 beta。
- 提供国内 / 海外完整离线包（内置安装器 1.0.1）、四种 UI / 语言载荷、独立分析器及 SHA-256 清单。
- 具体实现与历史演进见下文；大盘分析原型、游戏内 G 曲线及进一步机模适配不作为本版正式功能。

## 1.1.9-beta10 · 2026-09-22

- 黄色横滚虚线移至左右外侧各 25%，中间 50% 留空，水平时末端贴齐固定十字横轴，继续跟随实际 Roll 转动；显示尺寸不变。
- 移除 beta9 的全部 AP / AT 监测、伺服来源替换、黄圈、文字提示及断开闪烁，标准和兼容界面同步处理。操纵输入继续使用已选来源，显示鲜绿色标记。
- 保留 N1 反推 R / 红色数值、风、贴边按钮拖动与透明度、覆盖层折叠及既有缩放。
- beta9 已备份，本地开发版编号递增至 beta10，未上传 GitHub。

## 1.1.9-beta9 · 2026-09-22

> 历史行为：以下 AP / AT 功能已在 beta10 移除，横滚虚线已改为两侧外段。

- 贴边按钮独立纵向拖动，松开保存；透明度作用于字体，悬停恢复清晰。
- 覆盖层右上角折叠 / 名称条恢复，保留展开尺寸、位置、边缘缩放与设置入口。
- N1 反推 R 标识及红色数值；AP 伺服黄圈、AP 标识、绿色手动输入、对称半幅横滚虚线；油门 AT 工作状态。
- AP / AT 断开后以 0.5 秒周期闪红 5 次，重新接通取消。状态只读，数据失效 / 换机 / 回放不误报。
- 不挤占既有十字与仪表显示空间；标准版图形及兼容版文字路径分别验证。自定义机模与实际 X-Plane 动画仍待实飞验收。

## 1.1.9-beta8 · 2026-09-22

- GS ≥60 kt 地面滑跑正式计入评分：双侧 >5 m 且摆幅 >10 m 非红降一级；绝对偏差 >5 m 非红降一级，>7 m 至少黄色；超过9 m后连续3秒未回到±7 m内判红；连续5秒未回到±3 m内非红降一级。
- 每项只触发一次，不累计横向移动距离；普通处罚封顶黄色，低速脱离、离地、暂停、回滑与间隙打断连续判定。
- 异步跑道匹配前保留有上限的接地样本，避免漏判滑跑开头；中英文报告增加完整触发证据，最终保存时更新评级，网页说明同步。
- 新版本为本地开发交付；保留 beta7 备份，未上传 GitHub。

## 当前进度

| 项目 | 当前情况 |
| --- | --- |
| 当前正式版 | 1.1.9，2026-09-23 发布；上一正式版 1.1.8 |
| 开发周期 | 1.1.9 开发结束；beta1–beta10 共 10 个编号版本保留为历史 |
| 安装器 | v1.0.0 已发布；v1.0.1 随 1.1.9 正式离线包提供 |
| 插件发行组合 | Standard／Compatibility × CN／International，共 4 种；现有配置优先于包的默认语言 |
| 分析器 | 插件版与独立 HTML 同步更新，继续使用各自的偏好存储 |
| 最新验证记录 | beta10：96 项 LuaJIT／UI／录制与显示状态回归及 8 项滑跑专项；安装包检查见开发验证 |
| 尚未完成 | 滑跑规则实飞验收、FF777 实机输入验证、游戏内实时 G 曲线、完整平台与实机场景验收 |

## [1.1.9-beta7] - Unreleased · 相对 1.1.8 的累计更新

### 连续录制与报告

- 将原有接地／100 ft 短窗扩展为从下降进近 **2500 ft 地形 AGL** 至低速滑跑结束的连续记录；高度基准为离地高度，不按机场海拔过滤。已用海拔 6000 ft 场景完成离线启动检查。
- 增加低空载入、存档恢复后未经过 2500 ft 阈值的兜底：从实际有效片段开始，标记起始高度、前段缺失和数据间隙，不补造此前轨迹，不因缺段本身扣分。
- 全程以 10 Hz 记录可用的经纬度、AGL／MSL、IAS／GS、姿态、G、操纵、发动机及来源信息；补齐物理 FPM、VVI、轨迹 FPM 与来源、AoA、六路舵面、油门杆和卡位等通道。机模未提供的通道保留为缺失值。
- 风记录由接地瞬间扩展为时间序列，默认每秒记录，可设为 0.1–10 秒；保存风向、风速、相对风分量及区间均值、采到的峰值与峰值时刻，并与航迹对齐。
- **取消滑行道出口网络解析与出口匹配**。当前结束条件为接地后在地面、GS 严格低于 30 kt 连续两秒；恢复速度、离地、暂停或数据间隙会重置计时。`low_speed` 表示低速录制完成，不表示已确认脱离跑道。
- 即刻落地弹窗继续显示当时的结果，后台录制独立进行。TXT 前部保留接地摘要和 100 ft 节选，注明“完整记录见末尾”；完整数据层置于文件最后。
- 增加分块写入、采样与间隙信息、未完成片段保留、导出失败重试和片段恢复工具。最终文件使用临时文件提交，并保留原有报告备份。

### 游戏内界面与输入诊断

- 新增独立摇杆、油门、N1 三个覆盖层；支持尺寸、颜色、透明度和布局调整。屏幕侧边新增两个低干扰半透明入口，快速打开设置与落地记录。
- 调试模式显示俯仰／横滚／偏航的输入来源、数值与状态，支持自动选择、逐轴手动切换和保存机模偏好。有效的零输入不会被当成数据缺失，手动来源失效时不静默替换。
- 标准／兼容落地弹窗支持拖动并保存位置。标准版覆盖层和落地弹窗支持四边、四角拖拽缩放，保留原尺寸滑条、字号按钮及位置控制；设置和记录窗口继续使用原生窗口边框缩放。
- 操纵区域固定 **1:1 正方形**，缩放不拉伸横纵坐标；保留标题、风向／风速、YAW 和调试来源的显示空间。
- 摇杆覆盖层新增实时风吹向箭头，优先使用飞机当前位置的瞬时风，独立于日志风采样间隔逐帧刷新；失效时回退并标记来源，静风隐藏方向箭头。
- 风箭头固定长度：游戏内为 48 像素，箭头头部为 8 像素；不随风速、方向或组件尺寸拉伸。颜色为 ≤10 kt 绿色、>10 至 20 kt 浅金色、>20 kt 红色，并保持较淡透明度。
- **游戏内覆盖层不再显示 ILS**。beta3 曾加入的 LOC／GS 刻度、菱形和标签已在 beta7 移除，ILS 数据只用于记录与分析器复盘。

### 网页分析器与同步复盘

- 完整记录并入原图表和仪表布局，取消独立“完整记录 · 进近、滑跑与风”面板；支持 100 ft 聚焦、全程查看、时间窗缩放、横向浏览、播放和跟随。
- 原“跑道与准确触地点”改为 **触地俯视图**，在跑道与触地点后绘制实际滑跑路线。
- 新增 **落地俯视图**，显示完整进近与落地的横向航迹、跑道中线／LOC 几何参考，以及与时间游标对应的飞机位置。俯视图鼠标定位与其他图表同步。
- 右侧竖向细节图默认保留，按选定时间区间放大横向细节；默认显示 LOC 偏差，无 ILS 参考时显示跑道中线偏差，并保留风向切换。无跑道几何时明确提示缺失。
- 主参数图保留可选风速曲线；俯视图风以箭头绘制在对应时刻的飞机位置上，放大后仍显示可见区域内的风。风向模式使用居中的横向偏角，修复左右方向相反及持续顶风跨 ±180° 时出现假断层的问题。
- 网页风箭头改为固定 32 个 SVG 绘图单位，保留与游戏内一致的风速颜色分级；不随风速或地图倍率拉长。
- 统一新增控件与原有发光按钮风格。缩放仅由 Ctrl＋鼠标滚轮、缩放按钮或倍率控件触发；悬停只定位时间，不改变地图位置与图表量程。
- 保持 1.1.8 及旧开发记录可读；可靠对齐的旧 100 ft 节选可补充已有字段，缺失信息不当作零或虚构为完整数据。
- 完善 TXT 按钮选择与拖放入口：取消选择不再误报“没有找到 TXT”，选择事件先保留文件列表再清空输入；错误扩展名、文本路径和未交付文件分别提示，并支持拖放文件项回退。

### ILS 几何参考

- 从 XP 导航资料匹配跑道 LOC／GS，并与 XP 导航数据库核对，保存台站位置、航向、下滑角、高程和来源快照；不依赖机模收音机接口，不更改调谐或自动驾驶状态。
- 在落地俯视图绘制 LOC 参考；竖向蓝线显示飞机的横向偏差，风以独立箭头叠加。
- 分析器 XYZ 操纵区下方／右侧的 LOC／GS 仪表始终保留固定尺寸。有有效参考时显示四点、中线和偏差菱形；无参考、失效、地面或范围外时分别显示 **INOP**，不隐藏整个仪表、不显示失效菱形。
- ILS REF 属于导航资料推导的几何复盘参考，不是实测机载接收机信号；不模拟电波遮挡或实际覆盖，不猜测缺失的下滑角。旧记录没有对应快照时不能还原当时的 ILS 数据。

### 弹跳识别与最终结果

- 解除首次触地后 **6 秒**的弹跳截止时间，并解除与 100 ft 节选结束的绑定；在同一连续落地会话内监视较晚发生或持续较长的弹跳。
- 保留最小离地时间与高度增量／向上速度证据，过滤短暂接地信号抖动；不跨时钟回退、数据中断或不同会话拼接弹跳。
- 调整触地后的复飞区分：低空长弹跳不再仅因持续上升就被提前结束；复飞确认结合相对触地高度和持续爬升，录制仍保留高度与总时长保护。
- 首次有效二次触地沿用既有 FPM／G 分析和一次弹跳降级规则。确认后保持本次落地状态，避免后续弹跳被重新识别为另一份首次落地报告；本轮未重定义多次触地的逐次严重度评分。
- 最终 TXT 导出前刷新已变化的接地摘要，修复完整记录已有弹跳、前部仍沿用早期“未检测到弹跳”结论的问题；刷新失败保留原摘要并提供重试。
- 实飞样本中两次触地相隔 **6.15 秒**、离地约 **5.73 秒**，旧 6 秒窗口造成漏判；该样本已加入回归。分析器对有完整数据的旧记录补充漏判证据提示，保留原 TXT 与历史评分。

### 性能与开发交付

- 游戏覆盖层取消固定 20 Hz 刷新上限，按模拟器帧更新；复用数据句柄与绘图缓冲，只在位置或尺寸变化时提交窗口几何。显示刷新与 10 Hz 磁盘采样分离。
- 分析器合并同一浏览器帧内的鼠标事件；游标与动态仪表单独更新，静态图表、图例和序列不再随每次悬停重建。航迹缓存和空间索引减少长记录的重复遍历。
- beta4 受控离线基准中，约 2.4 万点记录的平均定位 JavaScript 耗时由约 32.97 ms 降至 0.152 ms；这是当时的 JS 工作量测量，不是 beta7 实机 FPS 承诺。beta7 已通过静态层复用与游标对齐回归。
- ILS 导航扫描采用分块读取、索引缓存和低频目标选择；长记录采用有界流式写入，避免每帧遍历或重写整段航迹。
- 未发布交付统一使用 `1.1.9-betaN`，安装清单、包名、安装后的主脚本、报告标题与分析器版本一致，安装器按 beta 数字正确排序。源码入口保持 `StarLux_LMM_v1.1.9.lua`。
- 历次修改保留源码／校验备份，开发工具、历史包与当前源码分区保存。当前安装包位于 `dist/1.1.9-beta7/`，四种载荷分别校验 18 个文件。

### 开发版本演进

以下为阶段记录；与累计说明冲突时，以 beta7 的当前行为为准。

| 阶段 | 主要变化 |
| --- | --- |
| 1.1.9 早期开发 | 工作区归档；独立覆盖层、输入诊断、2500 ft 长记录、风序列、统一时间轴；移除出口网络，采用低于 30 kt 两秒结束；完善导入与全程通道 |
| beta1 | 独立实时风叠加；正式启用开发小版本编号与安装器识别 |
| beta2 | 修复持续顶风曲线在 ±180° 附近的显示断层 |
| beta3 | XP ILS 几何参考、航迹与风箭头联动、四点仪表；游戏内 ILS 后于 beta7 移除 |
| beta4 | 保留并放大竖向细节图；优化鼠标定位和长记录渲染性能 |
| beta5 | 缩减操作区留白；覆盖层与落地弹窗增加边缘／四角缩放 |
| beta6 | 修正长方形操纵区，恢复横纵 1:1 等比例 |
| beta7 | 固定风箭头尺寸；移除游戏内 ILS；网页常驻 INOP 仪表；长弹跳识别与最终摘要修正 |

### 待完成与适用范围

- **滑跑评分已在 beta8 启用**：GS ≥60 kt 使用固定阈值、持续时间与双侧摆幅规则；按事件记录处罚，仍待实飞验证。既有触地点中线评价继续保留。
- FF777 等特定机模的摇杆来源仍待实机验证；已增加诊断与切换工具，不代表已修复所有专用输入接口。
- 游戏内实时 G 曲线及更多曲线覆盖层仍未完成。
- 兼容版覆盖层仍以英文数字显示为主，未达到标准版图形与边缘缩放等效；标准版新图形能力依赖 SDK 440。
- Windows 本地回归不能代替 Mac／Linux、不同显卡、多显示器、高 DPI 和全部机模的实测；安装器当前为 Windows 程序。beta7 的弹跳／复飞边界和实际拖拽手感仍待复测。
- 验证明细见 [开发验证](docs/StarLux_LMM_v1.1.9_开发验证.md)，后续安排见 [Roadmap](ROADMAP.md)。本文所列 86 项检查为最近一次 beta7 结果，不是本次文档整理新增的测试。

## Installer [1.0.1] - 本地维护版 · 2026-09-21

- 扩展旧文件清理：已确认属于 LMM 的旧 UI、空目录、版本 README 和旧主脚本纳入事务备份及替换流程，减少新旧文件混用；保留用户配置、飞行记录、机场缓存和未知用户文件。
- 随 1.1.9 本地开发包验证 beta 版本排序、四种变体发现、主脚本替换、安装回执、完整性诊断和保留配置修复。
- 该维护版随本地开发包提供；未在本阶段发布为线上安装器更新。

## Installer [1.0.0] - 2026-09-13

- 安装器首个正式版本，采用独立于插件的版本号；支持安装器自更新、下载校验、EXE 替换、重启确认与失败恢复。
- 增加插件完整性和兼容性诊断，识别缺失文件、内容不符、新旧主脚本／UI 混用及 XP 版本不匹配；提供修复、保留配置重装、纯净重装和卸载。
- 延续事务备份与回滚，保留飞行记录、机场缓存、共享 FlyWithLua 和其他脚本；纯净重装通过分析器页面一次性重置已知偏好，普通修复保留配置。
- 顶部新增中英切换；界面采用深蓝至浅蓝渐变，插件、健康和安装器状态独立显示。有更新时红色闪烁，确认最新时静态绿色，未知状态单独标记。
- 发布独立 `installer-v1.0.0` 自更新资产；插件 v1.1.8 发行中补充内置新安装器的 EN／CN 离线包。EN 压缩包使用无中文文件名，插件 1.1.8 内容保持不变。
- 上述发布日期与资产信息依据工作区保存的发布记录整理；本次仅更新文档。

## [1.1.8] - 2026-09-12

- 正式发布已经作者实机验证的 1.1.8beta 标准 UI；保留设置／落地记录两个菜单、五页功能分区、清晰滑块、统一中英文、原生中文字号和字重。此次正式版不修改 1.1.7 的落地算法。
- 提供 Standard（XP 12.4.4+）和 Compatibility（XP 12、12.4.4 之前）两个 UI 发行通道，每个通道有 CN／International 初始语言版本。兼容版主动跳过 SDK 440 字体初始化，传统设置和记录窗口使用英文控件，中文报告及标准字号中文弹窗保留；已有配置不覆盖。
- 安装器升级为 1.1.8，按语义版本统一识别本地与线上最高版本，为该版本所有变体加上 `【最新 / Latest】`；显示兼容范围、默认语言，保留双源下载、用户确认、文件校验、备份与回滚。
- 新增独立 `Starlux_Analyzer_落地分析器.html`，不依赖插件或外部脚本，使用独立浏览器存储命名空间；插件内仍使用 `LMM_Report_Reader.html`。
- 经作者授权，发布 5 个真实个人预设：默认、黑暗玫瑰、巨峰葡萄、未来展厅、水晶紫罗兰。只保留 RGB、缩放、布局和曲线选择，移除个人 ID、时间及废弃深浅模式；仅初始化没有保存列表的分析器，不覆盖已有预设。
- GitHub 首页使用作者新增的截图及原始 GIF，按动态演示、报告总览、原生设置／记录、配色与英文完整视图、原始数据复核分组；旧版展示仍可通过历史发布查看。
- Release 1.1.8 promotes the tested standard UI, adds explicitly labelled pre-12.4.4 legacy packages and CN/International defaults, bilingual Latest badges, a standalone analyzer with isolated preferences, five author-approved presets, refreshed screenshots and a GIF gallery. Existing settings/logs and landing algorithms are preserved. Legacy controls are English due to the old ImGui font atlas; Chinese reports and normal-size Chinese popups remain available.

## Installer [0.1.0 preview] - 2026-09-12

- 新增 Windows 10/11 x64 便携安装器 `StarLux_LMM_installer_安装器.exe`，自带 .NET 运行库；发行根目录仅 EXE 与 `version`，预置 1.1.8beta 全局语言修订版
- 自动发现 X-Plane 安装记录、Steam 库和常见目录，多安装实例由用户选择，支持手动指定根目录；中英文并列 UI 显示已安装版本、文件校验状态、即将安装版本、下载源及进度
- 并行核对 GitHub/Gitee Releases，按语义版本排序并合并同名资产，超时、下载损坏或失败时切换同版本备用源；离线本地包及单 EXE 联网安装均支持，明确提示降级与无发布方摘要的旧包
- 缺少 FlyWithLua 时询问是否下载官方固定提交内的 XP12 NG+ 2.8.14，或导入本地 NG+ ZIP；不误用 GitHub Releases 的 XP11 2.7.32，不自动升级既有 FWL，不启用示例脚本；国内依赖镜像需另行发布配置
- 安装前备份到 EXE 旁 `backup`，移出旧 LMM 主脚本，保留设置、落地记录、机场缓存和其他脚本；校验备份与写入文件，异常自动回滚，提供手动恢复与未完成事务检测、并发安装锁
- 严格相对路径／ZIP 检查、HTTPS 来源限制、哈希校验与大小上限；不跟随目录联接，不自动提权、不改系统安全设置、不运行下载脚本
- 清单预留 `native` 类型，限定安装至独立 `Resources/plugins/StarLux_LMM`，为未来 XPL 安装留出接口；当前不包含原生 LMM 插件
- 新增构建、打包、发行说明及离线集成测试；实测双源现有 1.1.4 ZIP 下载解析、官方 FWL 归档安装／恢复，所有安装测试仅使用临时模拟目录，未修改用户真实 X-Plane

## [1.1.8beta] - 2026-09-12

### Settings / records UI revision — 20260912-ui-refresh

- 恢复设置、落地记录两个独立 FlyWithLua 双语菜单与记录快捷命令；窗口及报告继续使用原全局语言选项
- 设置窗口改为通用／位置／配色／字体／工具五页，功能区使用卡片标题、边框与青绿色选中态，控件提供悬停反馈；限制超宽内容区域，修复文字宽度预算不一致造成的中文／English 短标签换行
- 滑块新增独立数值、清晰轨道、白色纹理拖柄；RGB 使用对应颜色；修正轨道拖动映射和滚动条抓取偏移，色彩预览块不参与点击
- 记录管理器区分工具、报告列表、删除确认及翻页区域；预留红色删除列，与记录行等高，窄窗口安全换行；不可用的翻页按钮禁用
- 删除设置内“磁航向猜测跑道”“真实字重”等冗余解释，保留必要状态与错误提示，详细字体诊断仅在 Debug 显示
- 16 项离线 LuaJIT 回归通过，增加双语三种宽度、页签、短标签、行对齐、记录打开／删除确认、滑块端点及菜单检查；接地弹窗绘制与排版模块哈希保持不变，字体、记录算法、网页和安装器 EXE 不变
- 新增独立 UI 发行包，只更新安装器 `version` 内容，不覆盖上一次发行包；真实模拟器目录未被写入

### Language revision / Fixed

- 根据用户实机日志定位并修正版本门槛：原实现混淆 XPLM API 版本值与 Log.txt 构建号，导致 12.4.4 被误判并回退；改以 SDK ≥ 440 与全部必需符号校验，日志输出真实版本值
- 完成唯一的全局中／英文选项：设置、记录管理、弹窗、数据库状态、TXT 输出和插件启动的网页分析器共用 `document_language`，旧 `interface_language` 仅用于无新键时迁移
- 设置窗口和记录管理窗口新增 Panel Graphics 中文渲染及鼠标交互适配，复用原设置操作逻辑；原生回调只渲染和排队输入，UI 操作在普通更新路径处理
- 补齐纯英文的语言说明、颜色／评级、位置名称、原生字体状态、字体大小及回退提示；切换语言刷新窗口标题与缓存的扫描错误提示
- 取消任何按字号自动切换为英文的逻辑：原生字号 10–32 都遵循全局语言；回退中文时使用标准 Unicode 字体，不覆盖保存字号
- FlyWithLua 入口改为语言中立的 StarLux LMM，记录管理入口放入可即时翻译的设置窗口
- 插件生成的网页桥接新增语言锁定，隐藏独立网页语言开关；直接打开 HTML 的独立模式仍可选语言；不修改历史 TXT
- 补齐报告中的等待测量／等待接地／等待分析、机场识别中及无持续降水等英文描述，新增静态报告文案与完整双语报告回归
- 新增设置／记录全局语言切换、按钮／滑块／滚动／窗口缩放、旧配置优先级及短 API 版本值测试。当前 13 项 LuaJIT 离线回归及 4 组网页语言切换用例通过；实机新绘制与交互仍需用户验收

### Added

- 独立 `StarLux_LMM_v1.1.8beta.lua` 和 `LMM_UI_118` 字体／绘制模块；不覆盖 1.1.7；网页本次仅调整全局语言联动
- 根据 SDK 440 官方头文件，以 FlyWithLua 的 LuaJIT FFI 创建 `contentType=1` 的 Panel Graphics 原生窗口；仅在该窗口回调内提交新图形和字体调用，不混入旧 OpenGL 绘制回调
- 原生弹窗新增 10–32 连续字号，中英文共用相同字号设置；常规、中等、加粗使用真正的独立字重，不再叠画模拟粗体
- 随包提供约 1 MB 的 Noto CJK 衍生 UI 字体子集和 OFL 许可证，覆盖当前脚本中文字形；额外尝试加载模拟器自带完整 CJK 字体作为未覆盖字符的后备，不复制或分发模拟器字体
- 设置增加 Automatic (SDK 440) / Compatibility 和当前后端／失败原因；新配置键 `popup_font_px`、`popup_renderer`，自动迁移原三档字号

### Changed

- 三种字重共同决定文字宽度和行高包络，按真实尺寸构建紧凑弹窗，切换粗细不改变边框尺寸或换行
- 按可用屏幕宽度执行 UTF-8 安全换行，长英文优先词边界；竖向／横向状态色条保留，去掉旧版固定最小宽度导致的大块留白
- 保留九宫格锚点、四向偏移、等级 RGB、字体颜色、中心线计算中／结果、报告闪烁三次及回放隐藏行为
- 新窗口使用 SDK 的全局桌面 boxel 坐标并考虑非零／负原点；实际多屏和高 DPI 缩放仍需实机验证
- 字体与窗口只在后端初始化时创建；布局按文字／字号／视口变化缓存，颜色、字重和闪烁不触发重新排版
- 极小窗口先临时缩小显示字号，不改保存值；仍放不下时末行显示省略号，完整数据保留在 TXT
- TXT 报告与加载日志版本标识更新为 1.1.8beta；FPM、G、弹跳、发动机接口和机场索引算法保持不变

### Safety / Validation

- 先探测 LuaJIT FFI、退出钩子、模拟器／SDK 版本及全部必需符号，再创建资源；模块、字体、接口缺失或可捕获绘制错误时回退旧版弹窗，不中断记录
- 退出／FlyWithLua 重载先销毁原生窗口，再释放 FFI 回调和字体；绘制回调内部不释放自身，相关 UI 函数关闭 JIT 编译以避免同步 C→Lua 重入问题
- 根据官方 SDK 440b1 C 头文件核对窗口结构：64 位大小 112 字节，`contentType` 偏移 88，`visible` 为 int；没有照抄 staging 页面中不同的字段名／Lua 类型
- 本机 X-Plane 12.4.4-b1 的 XPLM DLL 已确认导出必需的新字体函数；没有向运行中的模拟器注入或安装测试版
- LuaJIT 2.1 实际编译通过；主脚本顶层局部声明数量不增加；对既有函数进行精确比较，除 UI、UI 设置持久化和版本文本外无算法函数变化
- 9 项离线回归通过：ABI／Unicode／三字重尺寸／闪烁缓存、缺少符号／旧 SDK／缺字体回退、回调失败释放与重建、大小边界与中英文换行、真实字体覆盖和许可证、主脚本迁移／语言／预览／回放／清理、缺 FFI／退出钩子回退等
- 离线 SDK 测试替身不等于实机 Vulkan/Metal 渲染验证；安装后需按 `README_1.1.8beta.md` 完成预览、缩放和重载验收

## [1.1.7 测试版] - 2026-09-06

### Added

- 新增独立的 `StarLux_LMM_v1.1.7.lua`，保留 v1.1.6 文件不覆盖，便于稳定性测试和随时回退
- 新增持久化 `apt.dat` 字节索引：按 `scenery_packs.ini` 优先级记录机场所在数据源与机场头字节偏移，后续查询可直接定位机场块
- 新增 Lua 内部渐进建库状态机：仅在低速地面或平飞巡航阶段，以固定小块读取 `apt.dat`；进近、接地分析、跑道解析与报告写入期间自动暂停
- 新增分帧索引检查点写入与中断续建：首次建库无需单帧扫描完整机场库，退出后可从最近保存的字节位置继续
- 机场索引缓存升级为 v2 分片格式：每个 `apt.dat` 独立保存机场偏移与完成状态，启动时再按当前地景优先级合并
- 新增左上角低干扰数据库状态条：首次建库或缓存失效重建时显示“Starlux_LMM数据库初始化中”，完成后显示“Starlux_LMM数据库建立完成，祝你飞行愉快”；提示跟随数据输出语言切换中英文
- 数据库初始化与增量更新提示新增紧凑整数百分比，不另起进度条或占用额外屏幕行
- Debug 模式新增数据库与机场扫描字段：显示扫描／保存／暂停状态、平滑 FPS、负载档位、单步预算、机场条目数、数据源、字节进度、目标机场、索引直达／顺序回退路线及失败原因

### Changed

- 稳定测试网页分析器正式接替无版本号的 `LMM_Report_Reader.html`；原 v1.1.4 阅读器另存为 `LMM_Report_Reader_v1.1.4.html`，Lua 仍按原文件名直接打开当前分析器
- RGB 主题改为按实际背景逐项生成可访问文字、边框、交互面与图表色；所有参数曲线、操纵／舵面曲线和逐台发动机曲线会随主题实时重算高对比配色，不再依赖固定深浅色表
- 顶部设置栏从网页缩放对象中独立，页面缩放只作用于分析工作区；设置栏改为吸顶显示，并适度增大主题、预设、布局与语言控件及其弹出面板
- 机场导航点查询与最终跑道几何匹配算法保持不变；只替换中间的 `apt.dat` 机场块定位机制，索引命中后通过 `seek` 直接读取目标机场
- 索引尚未覆盖目标机场时继续使用原顺序扫描作为首次运行回退；完整索引确认不存在该机场时立即结束，避免无意义地遍历全部文件
- 索引缓存分别保存各数据源的路径、文件大小及多位置内容指纹；自定义机场包或 Global Airports 变化后只废弃对应分片，`scenery_packs.ini` 调序只重新合并优先级
- 索引缓存通过临时文件分帧写入并轮换安装，未完整写入的缓存不会在下次启动时被采用
- 渐进建库改为读取 XP12 平滑帧周期并动态分档：低帧率快速降低扫描量，高帧率缓慢增加扫描量；最高限制为每 0.05 秒四个 64 KiB 小块，防止高性能设备无限制占用主线程
- 分帧缓存写入同步采用相同负载档位，每步批量处理 512–2048 条机场索引记录；进近、接地和报告关键阶段的完全暂停规则保持不变
- 地景数据源发生变化时改为增量更新：新增或实际变化的 `apt.dat` 才会被扫描，已验证数据源直接复用；删除地景只移除对应分片，单纯调整 `scenery_packs.ini` 优先级只重新合并、不再扫描文件
- 增量更新期间左上角提示改为“数据库增量更新中／完成”，Debug 状态增加复用与待扫描数据源数量
- 数据库在内存索引可用时立即结束初始化提示，缓存随后继续原子落盘；缓存记录改为每帧批量拼接写入，避免低帧率下数万条记录逐条保存拖延数分钟
- 初始化百分比按本次实际待扫描的 `apt.dat` 字节量加权，已复用数据源不计入分母；扫描期间限制为 0–99%，全部分片完成后才进入 100% 完成状态

### Fixed

- 修复高亮度 RGB 主题下，正文、次要文字、卡片边界、图表网格、对比曲线及部分交互控件与浅色背景混在一起的问题
- 修复紫灰、粉棕等中间亮度 RGB 主题会迫使多条图表曲线同时退化为近白色的问题：绘图区改用连续继承主题色相的深色高对比仪表底，参数曲线不再通过降低饱和度换取对比度，并同步校正油门／动力、XYZ 输入、风向与姿态仪表的主／对比标色
- 移除主题算法中按单一亮度阈值整体切换文字明暗的分界，改为根据每个实际表面独立校正对比度，避免界面在中间亮度出现整页突变
- 修复大型 Global Airports 与低帧率场景下，从文件头按帧扫描可能在进近或触地后的时间预算内无法到达目标机场、最终漏记跑道的问题
- 避免为了修复机场读取而引入 SimBrief、FMS 目的地、外部程序或额外网络依赖
- 修复任意一个地景包新增、删除、重排或内容变化都会使整张机场索引失效、反复扫描大型 Global Airports 的问题
- 修复单个索引字节偏移校验失败时清空整个机场数据库的问题；现在仅废弃并重建对应数据源分片

### Validation

- 网页脚本通过 JavaScript 语法解析；使用纯黑、默认蓝、纯红、浅黄、纯白和浅蓝六组 RGB 种子完成静态对比度审计，主要文字均不低于 5.6:1，次要文字均不低于 5.2:1，参数曲线在绘图区内不低于 4.5:1
- 新增纯黑、默认蓝、纯红、浅黄、纯白、浅蓝及四组紫灰／粉棕种子的曲线色彩回归：20 组参数色均保持唯一，最低饱和度 71.4%，主曲线最低对比度 4.58:1，半透明对比曲线合成后最低对比度 3.14:1，常用 FPM／RA／G 三线最小 RGB 距离 128
- 已核对 65%／100%／135% 缩放代码路径仅修改分析工作区，顶部设置栏和右侧缩放控件保持原始尺寸；本地页面自动化预览受浏览器安全策略限制，需在交付后的本机浏览器中完成最终视觉验收
- 通过 Lua Language Server 静态诊断；新增函数全部挂接到既有 `log_tools` 表，未增加 FlyWithLua / Lua 5.1 主代码块顶层局部函数
- 已检查索引优先级、字节边界续建、完整文件结束、缓存源变更失效、索引命中校验失败后回退顺序扫描，以及进近关键阶段暂停条件
- 使用本机 XP12 Global Airports 实测 `KORD`、`KLGA` 与 `ZGNN` 的机场头字节位置并执行直接定位复核，三个偏移均能准确读回对应机场记录
- 已按本机 XP12 `DataRefs.txt` 核对 `sim/time/framerate_period`：该字段为完整模拟帧的平滑秒数，可用倒数计算 FPS；完成五档预算边界、快速降档／缓慢升档与最高吞吐限制回归
- 完成 v2 分片缓存场景回归：覆盖缓存不变、优先级重排、新增高优先级地景、已有地景内容变化、机场从自定义地景回退到 Global Airports，以及删除地景数据源
- 完成脚本字符串、长注释、括号与代码块静态平衡检查；顶层 `local function` 与顶层局部声明数量保持为 82 / 109，未触发此前的 Lua 5.1 主代码块局部变量风险
- 完成初始化百分比边界回归：覆盖首次建库、字节加权、多分片续建、排除已复用 Global Airports、99% 未完成保护、100% 完成及零字节数据源

## [1.1.5 测试版] - 2026-08-29

### Added

- 新增完整接地过程记录：首次接地不再立即终止轨迹；无弹跳时继续记录连续接地 2 秒，发生弹跳时覆盖离地、再次接触及最后一次接触后的 2 秒稳定段
- 新增首次接地后 8 秒安全上限、接触次数、空中阶段次数、完整轨迹时长和结束原因元数据
- 新增三轴玩家操纵输入采集：Pitch、Roll、Yaw 使用 X-Plane 标准归一化操纵 DataRef
- 新增六个实际舵面通道：Elevator 1/2、Aileron 1/2、Rudder 1/2 使用 XP12 `sim/flightmodel2/wing/*` 实际角度数组
- 新增最多四台发动机的逐台油门手柄与 N1 采样：正推统一使用标准 `cockpit2` 手柄比例，并按 `acf_en_type` 自动选择喷气反推或螺旋桨 Beta/反推范围；N1 使用 `sim/flightmodel/engine/ENGN_N1_` 且日志列固定存在
- 新增 ToLiss AirbusFBW 卡位适配：A320/A321/A330neo/A340 记录逐手柄 `IDLE / CL / FLX/MCT / TOGA / REV / MANUAL` 标签，并同步保存原始 `AirbusFBW/THRLeverMode` 供复核；其他机模没有已验证接口时写入 `NA`
- TXT 报告新增操纵数据状态、可用/缺失通道、首次接地快照及独立 10 Hz 操纵响应表；缺失值使用 `NA` 保持列对齐
- 报告阅读器新增三轴操纵响应图、地面接触区段、首次接地事件线，以及与主轨迹同步的悬停游标和数值提示；同一图内追加 Thrust 轨道，逐台显示油门、N1 与空客卡位色带

### Changed

- 0.25 秒轨迹在保持原有前 10 列兼容的基础上追加 G 与 Ground 列，并延长到完整接地过程结束
- 拉平曲率、下降率改善、FPM、固定 160 ms 主 G 和最终评分仍只使用首次接地及以前的数据；新增接地后数据与操纵数据仅用于复盘
- 阅读器升级到 v1.1.5，并继续兼容不含操纵表和扩展轨迹列的 v1.1–v1.1.4 日志
- 阅读器绘图区横向扩展；主轨迹与操纵响应改为同一复合区内上下排列并共用时间范围和左右边距，着陆数据复核移到操纵图下方
- 落地报告先在内存中完成组装，再以每帧 8 KiB 分块写入磁盘；“落地详细报告已生成”只在文件关闭成功后出现，减少报告完成瞬间的单帧磁盘写入卡顿

### Fixed

- 修复首版分帧写入实现新增多个顶层局部函数后触发 FlyWithLua 2.8.12 / LuaJIT 代码块限制、导致脚本在加载时被移入 `Scripts (Quarantine)`：新增写入状态和函数现全部挂接到既有状态表，不再增加主代码块局部变量

### Validation

- 通过 Lua Language Server 静态诊断、网页脚本语法检查、旧/新日志解析回归及合成弹跳轨迹的浏览器界面检查
- 通过正常接地、一次弹跳、多次弹跳和持续离地至 8 秒安全上限的状态机回归；修正模拟计时恰为 `0.000 s` 时稳定计时可能多一个采样周期的边界
- 已按本机 XP12 `DataRefs.txt` 核对标准输入 DataRef 的归一化范围和实际舵面数组的角度单位；跨机型符号与索引仍需在 X-Plane 内实飞确认
- 已按本机 XP12 `DataRefs.txt` 核对逐台油门、喷气反推、桨叶 Beta/反推、发动机数量与 N1 的类型和范围；并从本机 ToLiss A320/A321/A330neo/A340 插件确认 `AirbusFBW/throttle_input` 与 `AirbusFBW/THRLeverMode` 均存在
- 新旧操纵表解析回归通过：旧 10 列日志继续解析，新 23 列日志可正确读取油门、N1、卡位与原始模式；新增网页脚本通过语法检查，动力曲线仍需下一次 X-Plane 实测日志完成最终视觉验收
- 使用 2026-08-29 ZGNN / A20N 实测日志复核：解析到 52 个聚合轨迹点、123 个操纵样本和 9/9 完整通道；浏览器确认双图全宽上下排列、复核区顺序正确且无脚本告警

## [1.1.4 稳定测试版] - 2026-08-23

### Changed

- FPM 改为 AGL 锚点选择：以 5 ft 以下 AGL Theil-Sen 几何下降率为跨机模锚点，物理 FPM 与 VVI 中距离 AGL 最近且差值不超过 30 fpm 的值胜出，两者均超限时直接采用 AGL
- 物理候选优先使用接地前 100 ms 离地速度中位数，短窗缺样时使用 250 ms 物理 P25；AGL 缺样时依次回退到物理候选和 VVI
- G 主值改为第一次接触后固定 160 ms 的 P75 稳健载荷，取消滑动搜索最大窗以及全段 P75、局部包络、冲量估算之间取大值／加权的旧选择逻辑
- 冲量复算统一使用净加速度 `(G - 1.0)g`，物理平均载荷统一使用 `1.0 + Δv/(g·Δt)`；冲量闭合只作复核，不再直接抬高最终 G
- 精简普通 TXT 的“着陆数据复核”段与 X-Plane Log 摘要：保留最终 FPM/G、采用源、关键物理参考、样本质量和复核结论，移除容易混淆的内部中间量
- 报告阅读器升级到 v1.1.4：优先展示精简复核字段，同时继续识别 v1.1–v1.1.3 的旧字段和旧日志
- 使用三张 v1.1.4 实际阅读器截图替换旧版展示图，并将 README、FAQ、发布说明、MSFS 开发说明与发行预设统一到当前版本
- 新增独立路线图：v1.2.0 接入飞行员操纵输入曲线与阅读器多皮肤/色调；长期规划包括摆脱 FlyWithLua 的可移植重写、MSFS 移植以及 Starlux Black Box 专业完整航程记录路线

### Fixed

- 修复机场密集区只采用单个“最近机场参考点”导致相邻机场跑道漏识别：进近阶段低频缓存附近机场并在同一次 apt.dat 顺序扫描中顺带收集候选；主机场几何不匹配时才按原严格跑道包络复核缓存，不放宽容差、不使用磁航向猜测，也不增加任何 UI 提示
- 修复固定偏好物理 FPM 或 VVI 无法覆盖不同机模的问题，取消“物理主值＋特殊保护”的机模相关假设，统一改由 AGL 锚定三条测量链
- 修复冲量把接地前 G 基线当作物理零点，以及物理平均 G 缺少 1.0 G 重力基准，导致 G 复算失真的问题
- 修复 160 ms 局部载荷在整个压缩段滑动选最大窗口造成的系统性上偏

### Removed

- 移除 apt.dat 自动标牌字段的无效读取与跨阶段保存；该字段不参与跑道匹配、TXT 或网页绘制。进近灯字段仍保持完全跳过

## [1.1.3 测试版] - 2026-08-22

### Added

- 新增按 scenery_packs.ini 优先级读取 apt.dat 的真实跑道识别，使用跑道端点、触地坐标和触地地速向量完成几何匹配
- TXT 新增跑道来源、可信度、长度、宽度、路面，以及距入口、末端和中心线的触地点参考数据
- 报告阅读器支持真实跑道和触地点数据，并补齐 40 / 30 / 20 / 10 ft 无线电高度参考标记
- TXT 新增双端跑道编号、入口内移、防吹坪、标线类别、TDZ／REIL／中线灯／边灯及有符号中心线偏差
- 报告阅读器新增 FAA、英国和 EASA/ICAO 跑道标线分类绘制、等比例精确触地点、与轨迹时间轴对齐的跑道事件带及浅色主题

### Changed

- 完整继承 v1.1.2 的 FPM、G 值、弹跳和回放隔离算法；精准跑道解析完成后新增中心线评分修正：超过 7 m 非红降一级，超过 15 m 直接判定为 UNSTABLE
- 跑道数据改为在 5000–100 ft 下降进近阶段按帧预读并缓存，100 ft 以下暂停，触地后复用缓存或从现有文件偏移继续
- 精确跑道识别的落地后独立时间预算由 20 秒调整为 30 秒；进近预读拥有独立的 300 秒安全上限
- FlyWithLua 宏菜单恢复为双语“打开设置/Open Setting”与“落地记录/Landing Record”；设置窗口和落地记录管理器仍固定使用英文
- 设置项继续使用“Data output language”；中文 TXT、落地弹窗和分析网页仍跟随数据输出语言
- 移除 TXT、数学附录、调试摘要和报告阅读器中的“FPM 可信度”显示；保留明确的下降率采用源、采用原因与三链原始对照值，FPM 选择算法和评分不变
- 彻底移除阅读器“显示着陆上下文详情”折叠入口；相对风、接地姿态与跑道触地点改为默认展示
- 接地姿态改为 737 风格启发但不照搬的姿态仪：彩色移动地平线联合显示横滚与俯仰；对比记录仅叠加紫色虚线地平线、俯仰梯和滚转指针，避免双背景遮挡
- 跑道俯视图改为固定可读视角：物理起点、入口、接地点和剩余距离保持真实纵向比例，跑道长短由中心线、距离刻度和接地区标线密度表达；插件不再从 apt.dat 解析或向 TXT 写出进近灯字段，阅读器也不再解析或绘制进近灯
- 100 ft 图底部改用 GS 轨迹梯形积分反推跑道入口时间，与 RA 50 ft、接地共用横轴；GS 样本不足时回退到报告 GS 外推
- 对比模式扩展到相对风与接地姿态：风向使用实线／虚线双箭头；姿态仪叠加对比地平线，迎角侧视图叠加对比机翼与气流
- 迎角侧视图精简为机翼、弦线与相对气流，新增 7.5° 通用尾擦俯仰参考红线，并在下方刻度同步加入独立红色参考标记；该参考不等同于 AoA 限制或具体机型限制
- 阅读器整体提高可读性尺度：扩大页面最大宽度、侧栏、卡片间距、正文和辅助字号，并同步放大风向仪、姿态仪、跑道、100 ft 图表及其内部 Canvas 标签

### Fixed

- 修复 100 ft 轨迹末尾在接地时从约 11–15 ft 被强制跳到 0 ft 的截断：新日志在 140 ft 内提前预采样，触地后扣除飞机参考点接地高度并从校准后的 100 ft 重新裁切；阅读器会对旧日志的强制零点特征进行仅限高度轨迹的自动校准，FPM、姿态和评分保持原值

- 修复大型 Global Airports apt.dat 从触地后才开始扫描，导致在 20 秒内无法到达目标机场并输出“跑道识别超过时间预算 / NONE”的问题
- 修复触地初始化会无条件取消进近跑道读取，使预读结果无法跨越触地复用的问题
- 修复报告阅读器空轨迹面板、对比按钮强调状态、Roll 为 0 时基准线和触地点标记可见性问题
- 修复浅色模式下姿态数值、说明文字、图表曲线、RA 标记与跑道事件文字对比度不足的问题
- 修复海岸、悬崖或高架跑道上地形 AGL 突变导致 100 ft 轨迹从 63.5 ft 跳到 47.1 ft 等缺口：新日志改用飞机 MSL 高度减 apt.dat 机场标高，机场标高不可用时才回退地形 AGL
- 轨迹仍保留 10 Hz 原始采样，网页聚合间隔由 0.5 秒缩短为 0.25 秒；TXT 记录本次轨迹高度基准，旧 0.5 秒日志继续兼容
- 新增跑道纵向距离闭合检查；以入口内移 + 入口后接地点 + 剩余距离核对物理跑道全长，避免固定视角改变接地点比例

## [1.1.2 测试版] - 2026-08-16

### Changed

- 以稳定的 v1.1.1 为完整基线重建，不改变设置、回放隔离、双语报告、G 值算法和普通落地的三链判断
- 仅当原三链已选中物理轨迹、且 250 ms 物理 P25 超过 300 fpm 时，复用既有触地前 80 ms 离地物理速度中位数进行复核
- 80 ms 短窗与有效 VVI 相差不超过 100 fpm 时采用短窗物理值；差值超限或短窗样本不足时由 VVI 保守接管并降低可信度
- 中英文 TXT 与报告阅读器同步显示高下降率短窗复核来源；阅读器保持 v1.1.1 的稳定字段，不加入实验性诊断项
- FlyWithLua 设置窗口、宏菜单和落地记录管理器固定使用英文；设置项名称改为 `Data output language`
- 保留 `document_language` 配置键兼容性；中文 TXT、落地弹窗和分析网页继续跟随所选数据输出语言

### Fixed

- 修复重着陆越重、250 ms 窗口前段较大下降速度越容易抬高最终 FPM 的问题
- 恢复 Lua 固定查找的 `LMM_Report_Reader.html` 文件名，避免生成可视化报告时找不到模板
- 修复选择中文数据输出后，落地记录管理器向不含中文字形的 FlyWithLua ImGui 传入中文而出现乱码的问题

### Removed

- 移除未发布实验版的起落架受力、压缩量、压缩速率数组读取及接触边缘重建
- 移除实验版新增的逐帧临时表与接触探针，不新增 DataRef，不增加正常飞行阶段采样负担

## [1.1.1 测试版] - 2026-08-04

### Added

- 新增 Chinese / English 文档输出语言设置；FlyWithLua ImGui 设置窗口固定使用英文，避免中文字体兼容问题
- 独立报告阅读器新增语言开关，记忆用户选择；插件内打开报告时自动跟随插件语言
- 新增“纯净模式”，关闭落地数据窗和“落地详细报告已生成”自动提示，但仍正常保存 TXT
- 新增国内版与国际版初始化设置预设，两者只在 `document_language` 的默认值上不同
- TXT 原始报告现跟随“Document output language”输出完整中文或英文内容，文件名增加 `_CN` / `_EN` 语言标记
- 报告阅读器与 Lua 侧解析器兼容中英文两套字段和轨迹表，可混合载入、对比与复盘

### Changed

- 版本号更新为 v1.1.1，FlyWithLua 菜单使用中英文并列名称
- 设置窗口高度适配新增语言和纯净模式选项
- CFG 正式使用 `document_language`；旧版 `interface_language` 仍可读取，并在下次保存时自动迁移

### Fixed

- 进入和退出回放时主动清理报告完成提示，并取消未完成的落地后台任务
- 回放期间绘制层不再显示任何遗留的自动提示

## [1.1] - 2026-07-29

### Added

- 新增 5 ft 以下物理轨迹、VVI 与 AGL 几何轨迹三链下降率验证
- 新增停稳并持续 10 秒后显示和完全不自动显示两种弹窗模式
- 新增 X-Plane 回放隔离，回放期间停止采样、触发和报告写入
- 新增 160 ms 局部冲击包络，保留长行程压缩中的有效 G 载荷段
- 宏菜单“打开设置”和“落地记录”完成中文化
- 报告阅读器新增 9 项可选聚合参数、动态图例、同单位共享量程和同色虚化对比曲线

### Changed

- 三链两两差值均不超过 60 fpm 时采用物理轨迹；任一差值超限或样本不足时采用同窗 VVI
- 100 ft 拉平轨迹与最终显示使用同一下降率来源
- 中可信和长行程压缩 G 值采用全局 P75 与局部冲击包络的较大值
- 报告中的 FPM 来源改为更自然的“下降率取值”说明，算法与字段数量保持不变
- 更新 QA、README、中文使用说明与 v1.1 Release Notes

### Fixed

- 修复部分长行程起落架机型可能出现物理 FPM 与实际轨迹持续偏离的问题
- 修复中可信 G 分支可能因全局 P75 稀释短暂有效峰值的问题
- 修复回放触地状态反复切换时重复生成报告的问题


## [1.0] - 2026-07-24

### Added

- 新增 100 ft 后拉平轨迹、拉平曲率、轨迹震荡结论与 50 ft 图表参考线
- 新增 Bounce Landing 检测、第二次触地记录和既定评级降级规则
- 新增游戏内日志管理窗口、浏览器可视化、原始 TXT 下载和日志删除
- 新增风向、接地姿态及具备可靠几何数据时的跑道触地点示意
- 新增独立阅读器双记录对比、关键差值、主／对比交换及虚化轨迹叠加
- 日志文件名新增机型与 `RWY` 前缀
- 详细数学日志可选输出完整采样和可逆推导过程

### Changed

- 插件内阅读器与独立阅读器统一使用 `LMM_Report_Reader.html`
- Lua 通过本地桥接文件自动载入游戏内所选日志，不再维护第二套网页界面
- `Nice` 使用深蓝色、`Stable` 使用深绿色；评分阈值保持不变
- 将 `UNSTABLE 重着陆` 的中文表述调整为 `UNSTABLE 不良落地`
- 合并独立阅读器说明到总使用说明
- 正式发布包移除个人测试日志并归档所有旧版文件

### Fixed

- 修复 50 ft 聚合时间可能引用错误飞行阶段并产生异常超长拉平用时的问题
- 修复内置阅读器飞机示意与独立阅读器样式不一致的问题
- 修复旧版内置阅读器与独立阅读器长期分叉的问题

## [0.7.2] - 2026-07-23

### Added

- 新增 Nice、Stable、Attention、UNSTABLE 四级评分和对应标准色
- 低于 1500 英尺后每 5 秒监测实际机身降水与跑道摩擦状态，并保留最近三分钟快照
- 跑道摩擦明确为湿滑时显示“道面湿滑，注意摩擦力”；连续两次降水达到 5% 时显示“持续降雨，道面可能湿滑”
- TXT 报告新增判断来源、降水连续样本、冲量一致性、可信度、采样质量与分析耗时
- TXT 文件名新增推算跑道号，例如 `LMM_ZGNN_05_时间.txt`

### Changed

- FPM 与 G 分别分档并取较严重等级，公开评分阈值保持不变
- FPM 主数据改用触地前物理垂直速度的稳健分位值，VVI 保留为同窗对照和缺样备用值
- G 值按俯仰、横滚投影到垂直方向，并用冲量与实际速度变化进行高、中、低三级可信度判断
- 单帧 G 尖峰强制进入低可信度；物理样本不足或间隔过大时启用静态备用上限
- “触地时”弹窗改为“分析完成后立刻”，旧设置会自动迁移
- 使用 256 槽固定环形缓冲区，并将采集、速度分析、冲量分析和评分分散到不同帧
- FPM 从末端 80 毫秒中位数改为触地前 250 毫秒物理速度第 25 百分位，降低接地状态延迟造成的异常小值
- 游戏内状态色恢复旧版深绿、深橙和深红，并为 Stable 增加同亮度的深蓝色
- 更新报告评分说明、项目文档与安装文件名

## [0.7.1] - 2026-07-22

### Added

- 新增机型识别，并在游戏内数据窗与 TXT 报告中显示
- 新增落地机场识别，机场代码写入数据窗、报告正文及 TXT 文件名
- 根据触地磁航向推算跑道号，并明确标注为估算值
- TXT 写入成功后新增独立的“落地详细报告已生成”提示

### Changed

- 机场查询延后约 3 秒，TXT 报告写入延后约 8 秒，进一步避开触地关键阶段
- 数据窗扩展为六行，并分别协调横向与竖向布局
- 落地记录文件名调整为 `LMM_机场代码_时间.txt`

### Limitations

- 跑道号目前按触地磁航向推算，不区分平行跑道的 `L/R/C`

## [0.7.0] - 2026-07-21

### Added

- 新增横向与竖向两种落地数据窗口布局
- 横向布局使用左侧状态色粗边，竖向布局使用顶部状态色粗边
- 新增 25%、50%、100% 三档背景透明度选择并自动保存
- 设置文件新增 `popup_layout` 与 `panel_opacity` 字段
- 落地 TXT 记录新增窗口布局与透明度档位
- 仓库和发布包包含初始化 `LMM_Settings.cfg` 与 `LMM_Log` 实飞测试样本

### Changed

- DataRef 改为通过 XPLM 句柄直接读取，避免占用 FlyWithLua 共享 DataRef 表槽位
- 将相关状态集中到表中，规避 Lua 5.1 单函数 upvalue 上限导致的隔离问题
- 触地窗口改为在约 0.22 秒 G 值采集和最终评级完成后显示，消除先绿后黄/红的闪变
- 日志摘要和 TXT 写入延迟约 1.5 秒执行，避免与触地关键帧重叠造成卡顿和拖影
- 优化横向文字基线、竖向布局、状态色、粗边框与透明背景
- 移除文字阴影，使小字号显示更清晰
- 所有代码注释改为中文

### Fixed

- 修复 FlyWithLua 加载时因回调捕获变量过多而进入 Quarantine 的问题
- 修复触地弹窗先显示临时 STABLE 数值再快速改变颜色的问题
- 降低落地瞬间同步创建目录和写入文件造成的卡顿

### Compatibility

- 继续支持触地时或地速低于 30 kt 时显示
- 继续支持 30、60、120 秒显示时长、九宫格位置、调试信息和 5 秒预览
- 保留 v0.6 的 FPM/G 采样、评分阈值和所有落地记录字段

## [0.6.0] - 2026-07-20

### Added

- 独立的 FlyWithLua ImGui 设置窗口
- 触地时或地速低于 30 kt 时显示落地结果
- 30、60、120 秒三档显示时长
- 九宫格屏幕位置选择与 5 秒预览
- 设置自动持久化到 `LMM_Settings.cfg`
- 每次落地自动生成独立 UTF-8 中文 TXT 记录
- 日志记录速度、姿态、风况、评分阈值与 G 值测量细节
- 可绑定命令 `starlux/lmm/open_settings`

### Changed

- 使用 X-Plane 运行时间控制弹窗时长
- 整理插件名称、消息前缀与版本信息
- 保留并完善稳健 G 值和 FPM/G 一致性保护逻辑
